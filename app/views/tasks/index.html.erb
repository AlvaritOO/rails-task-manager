<br>
<br>
<div class="container">
  <h1>Todos las Tareas</h1>
    <ol>
      <% @tasks.each do |task| %>
        <li> <%= link_to task.name, task_path(task) %> - <%= link_to "Edit", edit_task_path(task) %> - <%= link_to "Delete", task_path(task), method: :delete, data: { confirm: "Are you sure?" } %> </li>
      <% end %>
    </ol>

  <%= link_to "Create new task", new_task_path %>
</div>